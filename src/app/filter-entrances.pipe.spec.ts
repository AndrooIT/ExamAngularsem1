import { FilterEntrancesPipe } from './filter-entrances.pipe';

describe('FilterEntrancesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEntrancesPipe();
    expect(pipe).toBeTruthy();
  });
});
