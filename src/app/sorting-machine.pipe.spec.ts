import { SortingMachinePipe } from './sorting-machine.pipe';

describe('SortingMachinePipe', () => {
  it('create an instance', () => {
    const pipe = new SortingMachinePipe();
    expect(pipe).toBeTruthy();
  });
});
